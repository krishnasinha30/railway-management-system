import { useEffect, useState } from 'react'
import { Search, ShieldBan, ShieldCheck } from 'lucide-react'
import api from '../api/axiosInstance'
import { Portal } from './TrainSearchPage'

export default function AdminUsersPage() { const [users, setUsers] = useState([]); const [query, setQuery] = useState(''); const [message, setMessage] = useState(''); const load = () => api.get(`/auth/users${query ? `?search=${encodeURIComponent(query)}` : ''}`).then(response => setUsers(response.data.data)).catch(() => setMessage('Unable to load users.')); useEffect(load, []); const toggle = async user => { try { const response = await api.put(`/auth/users/${user._id}/toggle-blocked`); setUsers(current => current.map(item => item._id === user._id ? response.data.data : item)); setMessage(response.data.message) } catch (error) { setMessage(error.response?.data?.message || 'Unable to change account status.') } }; return <Portal title="User management" eyebrow="Admin control room"><div className="mb-6 flex max-w-2xl gap-3"><div className="relative flex-1"><Search className="absolute left-4 top-3.5 text-slate-400" size={18} /><input className="field pl-11" value={query} onChange={event => setQuery(event.target.value)} placeholder="Search name, email, or phone" /></div><button onClick={load} className="rounded-2xl bg-ink px-5 text-white">Search</button></div>{message && <p className="mb-5 text-sm text-plum">{message}</p>}<div className="overflow-hidden rounded-3xl border border-ink/10 bg-white"><div className="divide-y divide-ink/10">{users.map(user => <div key={user._id} className="grid gap-4 px-6 py-5 md:grid-cols-[1fr_1fr_auto_auto] md:items-center"><div><p className="font-bold">{user.name}</p><p className="text-sm text-slate-500">{user.email}</p></div><div><p className="text-sm capitalize text-slate-600">{user.role}</p><p className="text-xs text-slate-400">Wallet ₹{user.walletBalance}</p></div><span className={user.isBlocked ? 'rounded-full bg-red-50 px-3 py-1 text-xs font-bold text-red-600' : 'rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700'}>{user.isBlocked ? 'Blocked' : 'Active'}</span><button onClick={() => toggle(user)} className="inline-flex items-center justify-center gap-2 rounded-xl border border-ink/10 px-3 py-2 text-xs font-bold">{user.isBlocked ? <ShieldCheck size={15} /> : <ShieldBan size={15} />}{user.isBlocked ? 'Unblock' : 'Block'}</button></div>)}{!users.length && <p className="p-10 text-center text-sm text-slate-500">No users found.</p>}</div></div></Portal> }
